# 当前任务与阶段计划

## 目的

本文件用于记录“当前正在推进什么、下一步做什么、哪些事情暂缓”，它是动态文件。  
如果计划变化，应优先更新本文件，而不是修改 `AGENTS.md`。

## 当前阶段判断

当前项目不再处于“找源码”阶段，而是处于：

1. 已有工作区可读源码
2. 已有外部 Python 回测最小脚手架
3. 正在把“示例策略理解”和“外部回测原型”两条线并行推进

补充状态：

4. 外部 Python 回测已经进入“实验版策略验证”阶段
5. 当前正式主标的已确认是 `510300.SH`
6. 当前目标不是跨标的通用策略，而是单标的可滚动校准策略
7. 但当前策略代码与说明书已出现偏差，现阶段应先恢复可追踪性和用户确认机制

## 已完成

### 环境与 API

- 已确认 `d:\python_envs\gd_qmt_env`（Python `3.12`）可以导入 `xtquant`
- 已完成大部分 `xtdata` API 实测
- 已确认：
  - 可用接口
  - 阻塞接口
  - 当前客户端不支持的接口

### QMT 内订阅行情验证

- 2026-04-23 在 QMT 实盘模式下实测跑通两种订阅：
  - `ContextInfo.subscribe_quote`：单只股票订阅（510300.SH tick + 1m），回调数据含价格/成交量/笔数/五档盘口
  - `ContextInfo.subscribe_whole_quote`：全推行情订阅（4只ETF），回调数据含完整快照（价格/成交量/笔数/五档盘口/量比/涨速）
- 验证程序位于 `code/run_qmt/` 目录：
  - `subscribe_quote_510300.py` — 单只订阅，支持 tick/1m 开关、日志文件开关
  - `subscribe_whole_quote.py` — 全推行情，支持按市场/按标的列表两种模式
- 关键发现：
  - QMT 内订阅回调函数只接收1个参数 `datas`，不需要 `ContextInfo`（与 init/handlebar 签名不同）
  - 全推行情推送速度略快于单只订阅（subscribe_quote），可能走独立推送通道
  - 全推行情数据是快照（含五档盘口），不是逐笔 tick

### 文档沉淀

- 已整理 API 可用性清单
- 已整理回测策略开发约束
- 已整理回测中的复权、指标、财务数据处理口径
- 已整理 ETF 低吸反弹主路线执行计划
- 已整理《策略说明书 v1：ETF 低吸反弹》
- 已整理 `510300.SH` 单标的数据分析结论：
  - `code/backtest/outputs/analyze_510300_research/510300单标的数据分析与研究结论.md`
- 已整理 `510300.SH` 低吸反弹事件定义与研究计划：
  - `报告/510300低吸反弹事件定义与研究计划.md`
- 已整理 `510300.SH` 低吸反弹事件前特征分析：
  - `code/backtest/outputs/analyze_510300_rebound_features/510300低吸反弹事件前特征分析.md`
- 已整理 `510300.SH` 低吸反弹事件日前窗口剖面分析：
  - `code/backtest/outputs/analyze_510300_event_profiles/510300低吸反弹事件日前窗口剖面分析.md`
- 已整理 `510300.SH` 多时间框架 V 字结构研究计划：
  - `报告/510300多时间框架V字结构研究计划.md`
- 已整理 `n5_r3` 首版 V 字结构评分模型：
  - `code/backtest/outputs/build_n5_r3_vscore_model/n5_r3首版V字结构评分模型.md`
- 已整理特征字段中文对照：
  - `报告/特征字段中文对照.md`

### 代码基础

- 已有最小外部 Python 回测脚本：
  - `code/backtest/minimal_stock_backtest.py`
- 已新增多 ETF 横向对比脚本：
  - `code/backtest/compare_etf_backtests.py`
- 已新增 `510300.SH` 单标分析脚本：
  - `code/backtest/analyze_510300_research.py`
- 已新增 `510300.SH` 低吸反弹事件扫描脚本：
  - `code/backtest/scan_510300_rebound_events.py`
- 已新增 `510300.SH` 事件前特征分析脚本：
  - `code/backtest/analyze_510300_rebound_features.py`
- 已新增 `510300.SH` 事件日前窗口剖面分析脚本：
  - `code/backtest/analyze_510300_event_profiles.py`
- 已新增 `510300.SH` 多时间框架 V 字结构分析脚本：
  - `code/backtest/analyze_510300_v_reversal_multiframe.py`
- 已新增 `n5_r3` 首版 V 字结构评分模型脚本：
  - `code/backtest/build_n5_r3_vscore_model.py`
- 该脚本已经实际运行成功
- 当前默认采用：
  - 前复权
  - 本地数据
  - Python 自算指标
  - 不接财务数据
- 已新增“当前代码实际运行策略事实”文档：
  - `报告/当前实际运行策略卡片.md`

### 示例策略输入

- 已获得工作区中的可读源码副本：
  - `python/多因子选股回测示例.py`

## 当前主线任务

### 主线 A：理解示例策略

目标：

- 基于工作区中的可读源码，明确示例策略到底实现了什么

当前要做的事：

1. 说明调仓频率
2. 说明股票池来源
3. 说明因子排序逻辑
4. 说明买卖条件
5. 区分“已实现”与“看起来打算实现但未完成”的部分

### 主线 B：验证源码与 QMT 实际运行的一致性

目标：

- 确认工作区里的源码副本，是否就是 QMT 最终实际加载和执行的版本

当前要做的事：

1. 确认 QMT 实际加载路径
2. 确认是否直接加载工作区文件，还是加载安装目录中的另一份文件
3. 如有必要，补回测日志、成交、持仓结果做交叉验证

### 主线 C：继续推进外部 Python 回测原型

目标：

- 在不依赖 QMT 内部回测框架的情况下，先把最小策略逻辑跑通

当前要做的事：

1. 以 `510300.SH` 为唯一主标的继续研究
2. 在不依赖 QMT 内部回测框架的情况下，持续完善外部 Python 原型
3. 先固定输出格式，再进入单标的分阶段分析与滚动校准设计

当前修正：

4. 当前不应继续直接修改策略逻辑
5. 应先让用户确认：
   - 当前实际运行策略是否接受为实验基线
   - 下一步是继续做单标的数据分析、回退对齐，还是重新定义说明书

## 当前不应作为第一优先级的事项

- 财务因子策略
- ETF / 可转债专项策略
- 个股超跌反弹选股策略
- 高频盘中复杂订阅策略
- 复杂框架化重构
- 在未确认加载版本前，直接把工作区源码当作最终 QMT 运行结果去下结论

## 当前冻结点

当前需要先冻结以下事实，不再模糊：

1. 当前代码里到底在跑什么
2. 当前代码和说明书差在哪里
3. 后续每一步策略改动都必须先说明计划，再等用户确认
4. 当前正式主标的是 `510300.SH`
5. 当前研究目标是“单标的可滚动校准”，不是“跨标的通用化”

## 下一步待确认顺序

以下事项当前都应视为“待用户确认”：

1. 是否接受 `报告/当前实际运行策略卡片.md` 作为当前实验基线
2. 是否保留当前实验版参数继续推进
3. 下一步是：
   - 做 `510300.SH` 单标的分阶段数据分析
   - 做 `510300.SH` 单标的小范围参数敏感度分析
   - 回退到更贴近说明书的版本
   - 重写策略说明书，使其与当前实验版对齐
4. 在用户确认前，不继续修改买点、卖点、持有天数、默认标的

补充说明：

- 第 3 项中的“单标的分阶段数据分析”与“参数敏感度分析”本轮已完成首版产出
- “事件样本定义”“事件扫描”“事件前特征分析”本轮也已完成首版产出
- “事件日前窗口剖面分析”本轮也已完成首版产出
- “多时间框架 V 字结构研究计划”与“多时间框架 V 字分析脚本”已新增
- `n5_r3` 首版 V 字结构评分模型已新增
- 当前真正待确认的，是是否继续进入：
  - 评分模型深化
  - 背景分 / 触发分拆分
  - 再之后的滚动校准框架设计

## 暂缓事项

- `download_financial_data` 的深入排障
- `XtQuantTrader` 交易连接链路验证
- `call_formula` / `call_formula_batch` 路线
- 复杂绩效分析与参数优化

## 更新规则

如果下面这些情况发生变化，应立即更新本文件：

- 当前主线任务变化
- 当前优先顺序变化
- 示例策略输入状态变化
- QMT 实际加载路径确认完成
- 外部 Python 回测原型推进到新阶段
- 当前实际运行策略卡片变化
- 用户确认的下一步实验方向变化
